'use strict';

angular.module('devoxxApp.controllers')
.controller('HomeController',
    ['$scope', '$rootScope', '$state',
    function ($scope, $rootScope, $state) {
      // $scope.$parent.checkRoles();
    }]);
